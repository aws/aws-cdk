To allow rewriting imports when generating v2 experimental modules, import of `CfnCluster` must be separate from import of `Cluster, ClusterParameterGroup, ClusterSubnetGroup, ClusterType`, and imported from its specific .generated location.