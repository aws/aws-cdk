/**
 * Downcase the first character in a string.
 *
 * @param str the string to be processed.
 */
export declare function downcaseFirst(str: string): string;
/**
 * Upcase the first character in a string.
 *
 * @param str the string to be processed.
 */
export declare function upcaseFirst(str: string): string;
/**
 * Join two strings with a separator if they're both present, otherwise return the present one
 */
export declare function joinIf(left: string | undefined, sep: string, right: string | undefined): string;
