{
  "name": "aws-cdk",
  "build": {
    "dockerfile": "Dockerfile"
  },

  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint"]
    }
  },
  "mounts": [
    "type=bind,source=${localEnv:HOME}/.ssh,target=/home/superchain/.ssh,readonly"
  ],
  // "postCreateCommand": "yarn install",
  "remoteUser": "superchain",
  "features": {
    "docker-in-docker": {
      "version": "latest",
      "moby": true
    }
  }
}
