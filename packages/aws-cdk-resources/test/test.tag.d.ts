import { Test } from 'nodeunit';
declare const _default: {
    'can use tags while modeling S3 buckets'(test: Test): void;
    'can use tag-like property bags while modeling S3 buckets'(test: Test): void;
};
export = _default;
