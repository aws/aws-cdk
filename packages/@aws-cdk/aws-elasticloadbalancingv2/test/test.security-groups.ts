import cdk = require('@aws-cdk/cdk');
import { Test } from 'nodeunit';

export = {
    'bidirectional security groups are automatically added'(test: Test) {
        // GIVEN




        test.done();
    },

    'adding the same targets twice also works'(test: Test) {
        test.done();
    },
};