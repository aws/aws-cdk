{
    "ResourceTypes": {
        "AWS::SSM::Association": {
            "patch": {
                "description": "rename +Name+ to +DocumentName+ to clearly indicate what the attribute is for",
                "operations": [
                    { "op": "move", "from": "/Properties/Name", "path": "/Properties/DocumentName" }
                ]
            }
        }
    }
}
