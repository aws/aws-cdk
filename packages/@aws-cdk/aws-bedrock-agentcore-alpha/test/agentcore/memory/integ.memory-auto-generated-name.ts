import * as integ from '@aws-cdk/integ-tests-alpha';
import * as cdk from 'aws-cdk-lib';
import * as agentcore from '../../../lib';

const app = new cdk.App();
const stack = new cdk.Stack(app, 'aws-cdk-agentcore-memory-auto-generated-name');

// Create Memory without specifying name (auto-generated)
new agentcore.Memory(stack, 'Memory', {
});

new integ.IntegTest(app, 'MemoryAutoGeneratedName', {
  testCases: [stack],
});
