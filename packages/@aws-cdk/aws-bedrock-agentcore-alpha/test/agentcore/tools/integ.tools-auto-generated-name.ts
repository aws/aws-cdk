import * as integ from '@aws-cdk/integ-tests-alpha';
import * as cdk from 'aws-cdk-lib';
import * as agentcore from '../../../lib';

const app = new cdk.App();
const stack = new cdk.Stack(app, 'aws-cdk-agentcore-tools-auto-generated-name');

// BrowserCustom without specifying name (auto-generated)
new agentcore.BrowserCustom(stack, 'Browser', {
});

// CodeInterpreterCustom without specifying name (auto-generated)
new agentcore.CodeInterpreterCustom(stack, 'CodeInterpreter', {
});

new integ.IntegTest(app, 'ToolsAutoGeneratedName', {
  testCases: [stack],
});
