{
  "Resources": {
    "Bucket": {
      "Type": "AWS::S3::Bucket",
      "Properties": {
        "BucketName": "some-bucket1231${!AWS::AccountId}232343453${ ! AWS::Region}452${!Immediate}234"
      }
    },
    "AnotherBucket": {
      "Type": "AWS::S3::Bucket",
      "Properties": {
        "BucketName": { "Fn::Sub": "unusued-bucket-fn-sub-${AWS::Region}-gibberish-${Bucket}-${!Literal}-${AWS::Region}" }
      }
    }
  }
}
