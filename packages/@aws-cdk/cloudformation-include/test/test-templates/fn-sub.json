{
  "Resources": {
    "Bucket": {
      "Type": "AWS::S3::Bucket",
      "Properties": {
        "BucketName": { "Fn::Sub": [ "unusued-different-sub-${Name}", { "Name": {"Ref" : "AnotherBucket" }} ]}
      }
    },
    "AnotherBucket": {
      "Type": "AWS::S3::Bucket",
      "Properties": {
        "BucketName": { "Fn::Sub": "unusued-bucket-fn-sub-${AWS::Region}" }
      }
    }
  }
}
