import * as core from 'aws-cdk-lib';
import { Construct } from 'constructs';
export declare class ServiceNetworkStack extends core.Stack {
    constructor(scope: Construct, id: string, props?: core.StackProps);
}
