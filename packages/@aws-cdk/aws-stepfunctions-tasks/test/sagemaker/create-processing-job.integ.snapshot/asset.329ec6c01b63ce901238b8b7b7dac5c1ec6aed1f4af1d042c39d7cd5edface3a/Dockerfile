# scratch is an empty, zero byte image `/` filesystem. We just need to build an image with
# at least 1 layer for it to be successfully published as an asset for integration tests
FROM scratch
COPY hello /
CMD echo 'Hello World'