import { Stack, StackProps } from '@aws-cdk/core';
import { Construct } from 'constructs';
export declare class StackSetPipelineStack extends Stack {
    constructor(scope: Construct, id: string, props?: StackProps);
}
